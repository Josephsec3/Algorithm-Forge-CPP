#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int lucky_nums[12] = {4, 7, 44, 47, 74, 77, 444, 474, 477, 747, 774, 777};
    int n; cin >> n;
    for (int i: lucky_nums){
      if (n%i==0){
        cout << "YES";
        return 0;
      }
    }
    cout << "NO";
}
